import React from 'react';

const Checkout = () => {
    return (
        <div>
            <h1 className='text-primary'>welcome to checkout page . only few more steps to go</h1>
        </div>
    );
};

export default Checkout;